import { AppRadius } from '../../../design-system';

export const PlatformRadius = {
  primary: AppRadius.panel,
  secondary: AppRadius.control,
  small: 'rounded-[8px]',
  huge: AppRadius.hero,
  massive: 'rounded-[40px]',
  full: AppRadius.pill,
};
